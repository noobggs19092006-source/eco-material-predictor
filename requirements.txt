# ── Core ML ───────────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0

# ── Explainability (for SHAP plots in evaluate.py) ────────────────────────────
shap>=0.44.0

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Dev & Testing ─────────────────────────────────────────────────────────────
pytest>=7.4.0
httpx>=0.25.0   # for FastAPI TestClient
```

---

## FIX 8 — Update `README.md` — Fix the Row-Count Contradiction

Find these lines in your README and replace them:

**Find this (the contradicting architecture block):**
```
285 rows → 70% Train (199) / 10% Val (29) / 20% Test (57)
```

**Replace with:**
```
800 rows (400 polymers + 400 alloys)
Training methodology: 5-Fold Cross-Validation
  → Reports mean ± std R² across 5 held-out folds
  → Final model trained on full dataset after CV validation
  → Fixed random_state=42 throughout (reproducible)